# Terminal Audio Player

A terminal based music player built with Node.js. It reads the local `songs/`
folder and plays tracks straight from the command line.

## Run

```bash
node player.js
```

Put your `.mp3` files inside the `songs/` directory.
